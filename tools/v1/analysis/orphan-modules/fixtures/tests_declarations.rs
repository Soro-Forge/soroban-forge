// Abbreviated from a real contract's test declaration file. Two modules that
// exist on disk were never added here, and a third was commented out during a
// debugging session and never restored.

mod admin;
mod funding;

#[rustfmt::skip]
mod coverage;

pub mod integration;

// mod settlement_upgrade_auth;
